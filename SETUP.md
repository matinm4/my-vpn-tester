# راه‌اندازی نسخه‌ی گیت‌هاب اکشن

از صفر تا اجرای خودکار — حدود ۱۰ دقیقه.

---

## گام ۱: ساخت نسخه‌ی کپی

در پوشه‌ی پروژه‌ی اصلی، در PowerShell:

```powershell
.\make-actions-copy.ps1
```

یک پوشه‌ی جدید کنار پروژه می‌سازد: `..\vtester-actions`

**فایل `config.yaml` عمداً کپی نمی‌شود** — چون `session_string` تلگرام شما
در آن است و این مخزن قرار است عمومی باشد. به‌جایش `config.ci.yaml` می‌رود که
همه‌ی رمزها را از سکرت می‌خواند.

---

## گام ۲: ساخت مخزن گیت‌هاب

مخزن جدید **عمومی** بسازید (public — تا اجرای اکشن نامحدود و رایگان باشد).

بعد در پوشه‌ی `vtester-actions`:

```powershell
cd ..\vtester-actions
git init
git add .
git commit -m "نسخه‌ی گیت‌هاب اکشن"
git branch -M main
git remote add origin https://github.com/USERNAME/REPO.git
git push -u origin main
```

---

## گام ۳: ساخت رمز حافظه

یک رمز قوی بسازید. در PowerShell:

```powershell
-join ((48..57) + (65..90) + (97..122) | Get-Random -Count 40 | % {[char]$_})
```

خروجی را کپی کنید. **این رمز را جایی امن نگه دارید** — اگر گمش کنید،
حافظه‌ی سرویس (لیست سیاه، تاریخچه، کش تلگرام) غیرقابل بازیابی می‌شود.

---

## گام ۴: تنظیم سکرت‌ها

در گیت‌هاب: **Settings → Secrets and variables → Actions → New repository secret**

### حتماً لازم است

| نام | مقدار |
|---|---|
| `VT_STATE_PASSPHRASE` | رمزی که در گام ۳ ساختید |

### فقط اگر تلگرام می‌خواهید

| نام | مقدار |
|---|---|
| `VT_TELEGRAM_API_ID` | عدد، از my.telegram.org |
| `VT_TELEGRAM_API_HASH` | رشته، از همان‌جا |
| `VT_TELEGRAM_SESSION` | نشست — گام ۵ |
| `VT_TELEGRAM_CHANNELS` | `@channel1,@channel2` — با کاما |

اگر این چهارتا را نگذارید، تلگرام خودکار خاموش می‌شود و سرویس فقط از
`subs.txt` کار می‌کند. هیچ خطایی نمی‌دهد.

سکرت جداگانه‌ای برای روشن/خاموش کردن تلگرام لازم نیست: `scripts/make-config.sh`
اگر `VT_TELEGRAM_SESSION` و `VT_TELEGRAM_API_ID` را ببیند خودش روشنش می‌کند.
برای رد کردن موقتی تلگرام در یک اجرای دستی، تیک **تلگرام رد شود** را در
صفحه‌ی Actions بزنید.

---

## گام ۵: ساخت session_string تلگرام

روی سیستم خودتان (نه گیت‌هاب):

```powershell
pip install telethon
```

بعد یک فایل به اسم `make_session.py` بسازید:

```python
from telethon.sync import TelegramClient
from telethon.sessions import StringSession

API_ID = 12345678          # عدد خودتان
API_HASH = "..."           # رشته‌ی خودتان

with TelegramClient(StringSession(), API_ID, API_HASH) as client:
    print()
    print("این رشته را در سکرت VT_TELEGRAM_SESSION بگذارید:")
    print()
    print(client.session.save())
```

اجرا کنید، شماره و کد تأیید را وارد کنید، و رشته‌ی چاپ‌شده را در سکرت
بگذارید. بعد **فایل `make_session.py` را پاک کنید**.

> ⚠ هرکس این رشته را داشته باشد به حساب تلگرام شما دسترسی کامل دارد —
> بدون رمز و بدون کد تأیید. هرگز آن را در فایل مخزن نگذارید.

---

## گام ۶: اولین اجرا

در گیت‌هاب: تب **Actions**

اول «بازبینی استخر» را انتخاب و **Run workflow** بزنید. باید چند دقیقه‌ای
تمام شود. اگر سبز شد، «کشف کانفیگ» را هم دستی اجرا کنید — این یکی طولانی‌تر
است (تا یک ساعت).

بعد از اولین اجرای موفق، زمان‌بندی خودکار شروع می‌شود:

- **بازبینی استخر** هر ۳۰ دقیقه
- **کشف کانفیگ** هر ۶ ساعت

---

## گرفتن خروجی

### راه ۱: Artifact (خصوصی)

در صفحه‌ی هر اجرا، پایین صفحه بخش **Artifacts**. فایل zip را دانلود کنید و
`top20_sub.txt` را در کلاینت import کنید.

خلاصه‌ی هر اجرا (بهترین پینگ، تعداد سالم، ۱۰ کانفیگ برتر) در همان صفحه
نمایش داده می‌شود بدون نیاز به دانلود.

### راه ۲: لینک ثابت عمومی (پیش‌فرض، فعال است)

هر اجرا خروجی‌ها را در شاخه‌ی `public` می‌نویسد. این لینک را در کلاینت به
عنوان **Subscription** اضافه کنید تا خودکار به‌روز بماند:

```
https://raw.githubusercontent.com/USERNAME/REPO/public/top20_sub.txt
```

`USERNAME/REPO` را با مال خودتان عوض کنید. لینک آماده هم پایین صفحه‌ی هر
اجرا (Summary) چاپ می‌شود، هم در README شاخه‌ی `public`.

فایل‌های شاخه‌ی `public`:

| فایل | چیست |
|---|---|
| `top20_sub.txt` | ۲۰ برتر، بیس۶۴ — برای Subscription |
| `top20.txt` | همان ۲۰ برتر، خام — برای کپی دستی |
| `batches/batch_XX.txt` | همه‌ی کانفیگ‌های سالم، دسته‌بندی‌شده |
| `batches/index.json` | فهرست دسته‌ها و تعداد کل |

**نام کانفیگ‌ها بازنویسی می‌شود.** قبل از نوشتن، نام اصلی (که معمولاً آی‌دی
تلگرام کانال یا متن تبلیغاتی است) با `🇩🇪 DE-01 | 210ms` عوض می‌شود: پرچم و
کد کشور، رتبه، پینگ. فقط برچسب نام تغییر می‌کند و هیچ فیلد کارکردی
(آدرس، پورت، UUID، SNI، path) دست نمی‌خورد.

اگر می‌خواهید نام خودتان هم اضافه شود، در `scripts/make-config.sh` مقدار
`output.name_tag` را بگذارید — نتیجه `MyName | 🇩🇪 DE-01 | 210ms` می‌شود.

**توجه:** این شاخه برای همه قابل دیدن است، پس کانفیگ‌های سالم شما عمومی
می‌شوند — همان چیزی که خواستید. اگر نمی‌خواهید، مرحله‌ی «انتشار عمومی» را
از هر دو ورک‌فلو حذف کنید و فقط از راه ۱ استفاده کنید.

---

## عیب‌یابی

| مشکل | راه‌حل |
|---|---|
| `VT_STATE_PASSPHRASE تنظیم نشده` | سکرت را اضافه کنید (گام ۴) |
| `هسته‌ی Xray پیدا نشد` | مرحله‌ی «نصب هسته‌ی Xray» باید **قبل از** «ساخت تنظیمات» باشد. هسته در `~/.local/bin` نصب می‌شود و از PATH پیدا می‌شود، نه از ریشه‌ی مخزن |
| `رمزگشایی حافظه ناموفق` | رمز عوض شده — شاخه‌ی `data` را حذف کنید تا از نو بسازد |
| `نشست تلگرام معتبر نیست` | `session_string` منقضی شده — گام ۵ را تکرار کنید |
| `هیچ کانفیگی پیدا نشد` | `subs.txt` خالی است یا لینک‌ها کار نمی‌کنند |
| اجرا خودکار شروع نمی‌شود | گیت‌هاب زمان‌بندی مخازن بی‌فعالیت را بعد از ۶۰ روز خاموش می‌کند — یک کامیت بزنید |
| شاخه‌ی `public` ساخته نمی‌شود | یعنی هیچ کانفیگ سالمی نبود؛ عمداً منتشر نمی‌شود تا لینک سالم قبلی خراب نشود. لاگ مرحله‌ی «انتشار عمومی» را ببینید |
| لینک عمومی ۴۰۴ می‌دهد | چند دقیقه بعد از اولین اجرای موفق فعال می‌شود؛ `USERNAME/REPO` را هم درست گذاشته‌اید؟ |
| اجرا تاخیر دارد | طبیعی است؛ زمان‌بندی گیت‌هاب تا ۱۵ دقیقه تاخیر دارد |

### دیدن جزئیات بیشتر

در فایل ورک‌فلو، به دستور `python live.py` فلگ `-v` اضافه کنید.

---

## مصرف

مخزن عمومی: **نامحدود و رایگان**.

با زمان‌بندی فعلی حدود ۵۰ اجرا در روز انجام می‌شود که مجموعاً چند ساعت
دقیقه‌ی محاسباتی است — برای مخزن عمومی هزینه‌ای ندارد.

اگر مخزن را خصوصی کنید، سقف ۲۰۰۰ دقیقه در ماه اعمال می‌شود و باید
زمان‌بندی را کم کنید (مثلاً استخر هر ۲ ساعت).

---

## تفاوت با نسخه‌ی ویندوزی

| | ویندوز | گیت‌هاب اکشن |
|---|---|---|
| دروازه‌ی فیلترشکن | دارد (۱۵ دقیقه مهلت) | ندارد — رانر اینترنت آزاد دارد |
| تلگرام در دورهای بعدی | خاموش | روشن |
| `xray.exe` | در پوشه‌ی پروژه | خودکار دانلود می‌شود |
| حافظه | `.cache/` محلی | شاخه‌ی `data`، رمزنگاری‌شده |
| concurrency | ۱۶ | ۲۴ |

کد پایتون هر دو یکی است.
