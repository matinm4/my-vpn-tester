# V-Tester — تست پینگ واقعی کانفیگ‌های V2Ray با هسته‌ی Xray

دو ابزار در یک پروژه:

| | |
|---|---|
| **`run.py`** | یک بار اجرا: لینک ساب → استخراج → تست → گزارش |
| **`live.py`** | سرویس دائمی: تلگرام + لینک ساب → تست → استخر ۲۰ تای زنده |

هر دو از یک هسته استفاده می‌کنند: **هر کانفیگ با `xray.exe` واقعی بالا می‌آید و
ترافیک واقعی از داخل تونل عبور می‌کند** تا پینگ و IP خروجی اندازه‌گیری شود.

اگر تازه شروع می‌کنید → [شروع سریع](#شروع-سریع).
اگر می‌خواهید خودکار از تلگرام بگیرد → [سرویس دائمی](#سرویس-دائمی-livepy).

## چرا «واقعی»؟

اکثر تسترها فقط یک TCP handshake به IP:پورت سرور می‌زنند. آن عدد چیزی درباره‌ی
سالم بودن کانفیگ نمی‌گوید: UUID غلط، path اشتباه، یا سروری که پورتش باز است ولی
پراکسی‌اش کار نمی‌کند، همه «سالم» گزارش می‌شوند.

اینجا برای هر کانفیگ:

1. یک فایل کانفیگ کامل Xray با یک اینباند SOCKS محلی ساخته می‌شود.
2. `xray.exe` با آن کانفیگ اجرا می‌شود و صبر می‌شود تا اینباند واقعاً پاسخ دهد.
3. یک درخواست HTTP واقعی از داخل همان تونل به `generate_204` زده می‌شود.
4. IP خروجی از داخل تونل خوانده می‌شود — اگر ترافیک واقعاً رد نشده باشد، عددی در نمی‌آید.
5. پروسه بسته و فایل موقتش پاک می‌شود.

یعنی یک کانفیگ فقط وقتی «سالم» است که واقعاً بایت از آن رد شده باشد.

در عمل این تفاوت بزرگ است: در اجراهای واقعی روی کانال‌های عمومی تلگرام، نرخ
موفقیت **۲ تا ۴ درصد** بود. تستر handshake همان‌ها را ۵۰٪+ سالم گزارش می‌کند.

## نصب

```powershell
python -m pip install -r requirements.txt
```

نیازمندی‌ها: پایتون ۳.۹+ و `xray.exe` در همین پوشه (نسخه‌ی موجود: 26.3.27).
`telethon` فقط برای دریافت از تلگرام لازم است؛ بدون آن `run.py` کامل کار می‌کند.

## شروع سریع

لینک سابسکریپشن را در `subs.txt` بگذارید (هر خط یک لینک) و اجرا کنید:

```powershell
python run.py
```

یا بدون دست‌زدن به فایل:

```powershell
python run.py --link "https://example.com/sub/token"
python run.py --link my_configs.txt          # فایل محلی کانفیگ
python run.py --link "vless://..."           # یک کانفیگ مستقیم
```

گزارش را باز کنید: `output\report.html`

## خروجی‌ها

| فایل | محتوا |
|---|---|
| `output\report.html` | گزارش کامل: کارت‌های آمار، سه نمودار، جدول قابل مرتب‌سازی و فیلتر، دکمه‌ی کپی |
| `output\results.json` | همه‌ی داده‌ی خام برای پردازش برنامه‌نویسی |
| `output\working.txt` | فقط لینک کانفیگ‌های سالم، مرتب از سریع‌ترین |
| `output\working_sub.txt` | همان لیست به صورت base64 — قابل import مستقیم در کلاینت |

## معیارهای گزارش‌شده

| ستون | معنا |
|---|---|
| **پینگ** | کمینه‌ی نمونه‌های گرم — رفت‌وبرگشت خالص شبکه از داخل تونل. عدد اصلی برای مقایسه. |
| **هندشیک** | درخواست اول (سرد) شامل برقراری تونل و هندشیک TLS. نشان می‌دهد اتصال اولیه چقدر طول می‌کشد. |
| **جیتر** | انحراف معیار نمونه‌ها. عدد بالا یعنی اتصال بی‌ثبات، حتی اگر پینگ پایین باشد. |
| **IP خروجی / کشور / ISP** | از داخل تونل خوانده می‌شود؛ مدرک اینکه ترافیک واقعاً رد شده. |

آستانه‌های کیفیت با واقعیت پراکسی تنظیم شده‌اند، نه با پینگ شبکه‌ی محلی:
عالی `<500ms` · خوب `<900ms` · متوسط `<1800ms` · ضعیف بالاتر از آن.

## تنظیمات

همه چیز در `config.yaml` با توضیح فارسی است. پرکاربردترها:

```yaml
test:
  concurrency: 16      # چند کانفیگ همزمان
  attempts: 3          # چند درخواست به ازای هر کانفیگ
  attempt_timeout: 8.0 # تایم‌اوت هر درخواست
  retries: 1           # تلاش مجدد کل کانفیگ در صورت شکست
```

هر مقداری را می‌شود با آرگومان خط فرمان هم موقتاً override کرد:

```powershell
python run.py -j 32 --attempts 5 --timeout 10
python run.py --limit 50 --fresh              # تست سریع ۵۰ تا، بدون کش
python run.py --only vless,trojan             # فقط این پروتکل‌ها
python run.py --allow-insecure                # خطای گواهی TLS نادیده گرفته شود
python run.py --log-level warning -v          # عیب‌یابی: لاگ خود هسته
python run.py --help                          # فهرست کامل
```

### تنظیم concurrency

هر کانفیگ یک پروسه‌ی مستقل `xray.exe` می‌گیرد (حدود ۲۵ مگابایت رم).
۱۶ تا برای سیستم معمولی خوب است. بالای ۳۲ تا اعداد پینگ نویزی می‌شوند چون
خود پهنای باند محلی گلوگاه می‌شود — برای سنجش دقیق، عدد را پایین بیاورید.

### تنظیمات پیشرفته‌ی Xray

هر کلیدی که در `xray.extra_outbound` بگذارید مستقیم داخل outbound هر کانفیگ
merge می‌شود؛ برای چیزهایی که پارسر پوشش نمی‌دهد:

```yaml
xray:
  extra_outbound:
    streamSettings:
      sockopt:
        tcpKeepAliveIdle: 100
```

## کش و از سرگیری

نتیجه‌ی هر کانفیگ **بلافاصله** روی دیسک نوشته و fsync می‌شود
(`.cache\results.jsonl`). اگر برنامه قطع شود — Ctrl+C، کرش، یا قطع برق —
اجرای بعدی فقط کانفیگ‌های تست‌نشده را تست می‌کند.

Ctrl+C اول: تست‌های در جریان تمام می‌شوند، گزارش ساخته می‌شود، و کانفیگ‌های
نرسیده دست‌نخورده می‌مانند (به‌عنوان «ناموفق» ذخیره **نمی‌شوند**).
Ctrl+C دوم: خروج فوری.

```powershell
python run.py --fresh              # کش پاک شود، همه از نو
python run.py --cache-ttl 6        # نتایج قدیمی‌تر از ۶ ساعت دوباره تست شوند
python run.py --retest-failed      # ناموفق‌های کش‌شده هم دوباره تست شوند
python run.py --no-cache           # کش کاملاً غیرفعال
```

## تشخیص کانفیگ تکراری

اثر انگشت از پارامترهای اتصال ساخته می‌شود: پروتکل، آدرس، پورت، UUID/رمز،
شبکه، لایه‌ی امنیت، SNI، هدر Host، path، serviceName و کلیدهای REALITY.

**نام کانفیگ در اثر انگشت نیست** — پس دو کانفیگ کاملاً یکسان با اسم متفاوت
(که در سابسکریپشن‌های تجمیعی خیلی رایج است) تکراری تشخیص داده می‌شوند.

## پروتکل‌های پشتیبانی‌شده

| ورودی | وضعیت |
|---|---|
| `vmess://` | JSON بیس۶۴ و فرمت URI |
| `vless://` | شامل REALITY، XTLS Vision، flow |
| `trojan://` | ✓ |
| `ss://` | هر سه شکل رایج + Shadowsocks 2022 |
| `socks://` `http://` | با و بدون احراز هویت |
| YAML کلش | `proxies:` برای انواع بالا |
| `hysteria2://` `tuic://` `ssr://` | رد می‌شوند — هسته‌ی Xray پشتیبانی نمی‌کند |

ترنسپورت‌ها: `tcp` (شامل http obfs)، `ws`، `grpc`، `httpupgrade`، `xhttp`، `kcp`.

دو نکته درباره‌ی Xray 26 که این نسخه با آن هماهنگ شده:

- ترنسپورت `h2` حذف شده و به XHTTP در حالت `stream-one` منتقل شده — لینک‌های
  قدیمی `type=http` خودکار مهاجرت می‌شوند.
- `header` و `seed` در mKCP حذف شده‌اند و جایشان `finalmask` و `mkcp-aes128gcm`
  آمده — نگاشت خودکار انجام می‌شود.
- ترنسپورت خام `quic` حذف شده و معادل دقیقی ندارد، پس این کانفیگ‌ها به‌جای تست
  غلط، صریحاً «پشتیبانی‌نشده» گزارش می‌شوند.

## اگر همه‌ی کانفیگ‌ها ناموفق شدند

1. `python run.py --log-level warning -v` — لاگ خود هسته را نشان می‌دهد.
2. اگر خطا «تایم‌اوت» است: ممکن است `test.latency_url` از شبکه‌ی شما در دسترس نباشد.
3. اگر لینک سابسکریپشن خودش فیلتر است: `--fetch-proxy socks5h://127.0.0.1:10808`
4. اگر خطای گواهی TLS دارید: `--allow-insecure`
5. `--timeout 15 --start-timeout 15` برای شبکه‌های کند.

## ساختار پروژه

```
run.py                  نقطه‌ی ورود
config.yaml             همه‌ی تنظیمات
subs.txt                لینک‌های سابسکریپشن
vtester/
  cli.py                آرگومان‌ها و خروجی کنسول
  orchestrator.py       جریان کار: بارگذاری → حذف تکراری → تست → گزارش
  sources.py            دانلود سابسکریپشن، base64/JSON/YAML، استخراج لینک
  links.py              پارس هر لینک به ProxyConfig
  xraycfg.py            ساخت JSON هسته‌ی Xray
  runner.py             اجرای پروسه، سنجش پینگ واقعی، تشخیص IP
  cache.py              کش JSONL با نوشتن فوری
  geo.py                غنی‌سازی کشور/شهر/ISP
  report.py             خروجی JSON و رندر HTML
  procguard.py          Job Object ویندوز — هیچ پروسه‌ی یتیمی نمی‌ماند
  models.py             ProxyConfig و TestResult
  templates/report.html قالب گزارش (خودبسنده، بدون CDN)
```

## نکته‌ی امنیتی

`output\` و `.cache\` حاوی UUID و رمز کانفیگ‌های شما هستند. اگر پروژه را
جایی به اشتراک می‌گذارید، این دو پوشه را حذف کنید.

---

# سرویس دائمی (`live.py`)

از کانال‌های تلگرام و لینک‌های ساب کانفیگ جمع می‌کند، تست می‌گیرد، و ۲۰ کانفیگ
برتر را همیشه زنده نگه می‌دارد.

```powershell
python live.py                    # سرویس دائمی
python live.py --once             # یک دور کامل و خروج
python live.py --pool-only        # فقط بازبینی ۲۰ تای برتر
python live.py --help             # فهرست کامل
```

## راه‌اندازی تلگرام

در `config.yaml`:

```yaml
telegram:
  enabled: true
  api_id: 12345678                 # از my.telegram.org
  api_hash: "..."
  session_string: "..."            # نشست ساخته‌شده (StringSession)
  channels:
    - "@your_channel"
    - "@another_channel"           # هر تعداد کانال
  days_back: 30                    # چند روز عقب خوانده شود
```

اگر `session_string` ندارید، یک بار این را اجرا کنید و کد تلگرام را وارد کنید:

```powershell
python -c "from telethon.sync import TelegramClient; from telethon.sessions import StringSession; print(TelegramClient(StringSession(), API_ID, 'API_HASH').start().session.save())"
```

خواندن هر کانال کش می‌شود (`.cache\telegram_state.json`) — پیام‌های خوانده‌شده
دوباره خوانده نمی‌شوند، پس دور دوم خیلی سریع‌تر است.

## دو چرخه‌ی مستقل

| چرخه | فاصله | کار |
|---|---|---|
| **کشف** | هر ۶۰ دقیقه | تلگرام + لینک ساب → کانفیگ جدید → تست |
| **بازبینی استخر** | هر ۳۰ دقیقه | ۲۰ تای برتر را دوباره تست و مرده‌ها را جایگزین می‌کند |

قطعی تلگرام کل سرویس را نمی‌خواباند — با لینک‌های ساب ادامه می‌دهد. اگر هر دو
منبع بیفتند، چرخه‌ی استخر همچنان کانفیگ‌های موجود را تازه نگه می‌دارد.

```yaml
live:
  discovery_interval_minutes: 60
  pool_interval_minutes: 30
```

## دروازه‌ی فیلترشکن — مهم

دریافت از تلگرام به **فیلترشکن روشن** نیاز دارد. تست پینگ به **فیلترشکن خاموش**.

این دو ناسازگارند: اگر تست با فیلترشکن روشن اجرا شود، ترافیک تست از تونل
فیلترشکن رد می‌شود و **همه‌ی کانفیگ‌ها الکی سالم به نظر می‌رسند** — چون در واقع
فیلترشکن شما دارد کار می‌کند، نه آن کانفیگ.

پس بعد از مرحله‌ی دریافت، سرویس می‌ایستد:

```
  IP فعلی (با فیلترشکن): 63.250.59.96 (DE)
  ...
  ┌──────────────────────────────────────────────────────────────┐
  │  مرحله‌ی دریافت تمام شد.                                      │
  │  حالا فیلترشکن را خاموش کنید و Enter بزنید.                   │
  │  مهلت: ۱۵ دقیقه. بعد از آن اجرا متوقف می‌شود و                │
  │  کانفیگ‌های دریافت‌شده ذخیره می‌مانند.                         │
  └──────────────────────────────────────────────────────────────┘

  تغییر مسیر خروجی تشخیص داده شد: 63.250.59.96 (DE) ← 37.156.154.158 (IR)
```

**نیازی به زدن Enter نیست.** سرویس هر ۱۰ ثانیه IP عمومی را چک می‌کند؛ به‌محض
اینکه فیلترشکن را خاموش کنید خودش می‌فهمد و ادامه می‌دهد. اگر Enter بزنید ولی
IP عوض نشده باشد، هشدار می‌دهد که فیلترشکن هنوز روشن است.

بعد از ۱۵ دقیقه: اجرا متوقف می‌شود. کانفیگ‌ها در پایگاه داده مانده‌اند؛ فیلترشکن
را خاموش کنید و دوباره اجرا کنید — مستقیم می‌رود سراغ تست همان‌ها.

```yaml
vpn_gate:
  enabled: true
  wait_minutes: 15
  require_ip_change: true    # false = صرف زدن Enter کافی است
  probe_every_seconds: 10    # هر چند ثانیه IP چک شود
```

اگر فیلترشکن از قبل خاموش است: `python live.py --skip-gate`

### تلگرام در سرویس دائمی

به همان دلیل بالا، تلگرام فقط در **دور اول** خوانده می‌شود (که خودتان حاضرید).
دورهای بعدی فقط از لینک‌های ساب می‌گیرند — سرویس دائمی نمی‌تواند هر ساعت منتظر
بماند شما فیلترشکن را روشن/خاموش کنید.

برای کانفیگ تازه از تلگرام، هر وقت خواستید جدا اجرا کنید:

```powershell
python live.py --once
```

اگر اینترنت شما بدون فیلترشکن به تلگرام دسترسی دارد:
`live.telegram_in_service: true`

## خروجی‌ها

| فایل | محتوا |
|---|---|
| `output\top20.txt` | ۲۰ کانفیگ برتر زنده، مرتب از سریع‌ترین |
| `output\top20_sub.txt` | همان به base64 — **این را در کلاینت import کنید** |
| `output\batches\batch_01.txt` | رتبه ۱ تا ۲۰ |
| `output\batches\batch_02.txt` | رتبه ۲۱ تا ۴۰ (و الی آخر) |
| `output\batches\index.json` | چند دسته نوشته شد، چند تا جا ماند |
| `output\pool_history.json` | تاریخچه: کی وارد شد، کی خارج شد، چرا |
| `output\report.html` | گزارش کامل (همان گزارش `run.py`) |
| `output\results.json` | داده‌ی خام |

سرصفحه‌ی هر فایل دسته‌ای رتبه‌اش را می‌گوید:
`# رتبه 1 تا 20 (از 100 کانفیگ سالم)`

فایل‌ها **اتمی** نوشته می‌شوند — اگر دقیقاً وسط بازنویسی import کنید، فایل
نیمه‌نوشته نمی‌بینید. دسته‌های کهنه هم پاک می‌شوند: اگر تعداد سالم‌ها از ۱۰۰ به
۴۰ برسد، `batch_03` تا `batch_05` حذف می‌شوند نه اینکه با داده‌ی قدیمی بمانند.

```yaml
pool:
  size: 20          # اندازه‌ی استخر برتر
  batch_size: 20    # چند کانفیگ در هر فایل دسته‌ای
  max_age_hours: 24 # نتیجه‌ی قدیمی‌تر از این در استخر حساب نمی‌شود
```

### تاریخچه‌ی استخر

`pool_history.json` هر ورود و خروج را ثبت می‌کند:

```json
{
  "current": [
    {"rank": 1, "name": "@FarazV2ray", "latency_ms": 207, "country_code": "DE"}
  ],
  "changes": [
    {"at_human": "2026-08-07 00:25:43", "action": "removed",
     "name": "@W570W", "reason": "جای بهتری گرفت"},
    {"at_human": "2026-08-07 00:25:43", "action": "added",
     "name": "@FarazV2ray", "latency_ms": 207}
  ]
}
```

دلیل خروج دو حالت دارد: «دیگر سالم نیست» (افتاده) یا «جای بهتری گرفت» (کانفیگ
سریع‌تری پیدا شده).

## حافظه‌ی کانفیگ‌های خراب

هر کانفیگ شمارنده‌ی شکست متوالی دارد. بعد از **۳ شکست پشت سر هم**، ۲۴ ساعت
کنار گذاشته می‌شود و اصلاً تست نمی‌شود — حتی اگر دوباره از تلگرام یا لینک ساب
بیاید. بعد از ۲۴ ساعت یک شانس دوباره می‌گیرد (سرورها گاهی برمی‌گردند).

**یک تست موفق پرونده را کامل پاک می‌کند** — هم شمارنده صفر می‌شود و هم تعلیق
برداشته می‌شود، حتی اگر هنوز به ۲۴ ساعت نرسیده باشد.

```yaml
health:
  blacklist_after_failures: 3
  blacklist_hours: 24
```

این مکانیزم از دور سوم به بعد اثرش را نشان می‌دهد: با نرخ موفقیت ۳٪، بیشتر
کانفیگ‌های مرده کنار می‌روند و دورها به‌مرور کوتاه‌تر می‌شوند.

## انبار و اولویت تست

هر کانفیگی که کشف شود در پایگاه داده ثبت می‌شود، حتی اگر همان دور تست نشود.
در هر دور، بعد از کانفیگ‌های تازه، از **انبار تست‌نشده‌ها** هم برداشته می‌شود:

```
5000 کانفیگ تست‌نشده از انبار اضافه شد (1426 تای دیگر در دورهای بعد)
برای تست: 5000 · از قبل معلوم: 220 · لیست سیاه: 0
```

کانفیگ‌های تازه‌تر اولویت دارند. این یعنی هیچ کانفیگی گم نمی‌شود — چه از
مهاجرت آمده باشد، چه از دوری که وسطش قطع شده، چه از سقف دور قبل.

```yaml
live:
  max_untested_per_cycle: 5000   # سقف برداشت از انبار در هر دور
  batch_test_size: 0             # سقف کل تست در هر دور (۰ = بدون سقف)
  retest_after_hours: 12         # نتیجه‌ی قدیمی‌تر از این دوباره تست شود
```

## مقیاس

پایگاه داده SQLite است (`.cache\configs.db`). اعداد واقعی با ۲۰۰٬۰۰۰ کانفیگ:

| عملیات | زمان |
|---|---|
| کوئری ۲۰ تای برتر | **۱۶ میلی‌ثانیه** |
| درج ۲۰۰ هزار کانفیگ نو | ۹۷ ثانیه |
| درج همان‌ها بار دوم (تکراری) | **۲۵ ثانیه** |

سه چیز مقیاس را نگه می‌دارد:

- ایندکس روی کوئری استخر و لیست سیاه
- کانفیگ بدون تغییر دوباره سریال نمی‌شود (فقط `last_seen_at` به‌روز می‌شود)
- جدول نتایج خودکار هرس می‌شود: برای هر کانفیگ فقط ۵ نتیجه‌ی آخر می‌ماند

```yaml
store:
  keep_results_per_config: 5
  prune_after_days: 30
```

## مهاجرت خودکار

اولین اجرا این‌ها را یک بار وارد پایگاه داده می‌کند:

- کش JSONL قدیمی (`.cache\results.jsonl`)
- خروجی اسکریپت تلگرام (`recive_config_from_telegram\configs_output.json`)

اسکریپت قدیمی دست‌نخورده می‌ماند و همچنان جدا قابل اجراست. مهاجرت با یک فایل
نشانه (`.cache\.migrated_*`) علامت می‌خورد و تکرار نمی‌شود.

## توقف

`Ctrl+C` اول: کار جاری تمام می‌شود، خروجی‌ها ذخیره می‌شوند، تمیز خارج می‌شود.
`Ctrl+C` دوم: خروج فوری.

هیچ پروسه‌ی `xray.exe` یتیمی نمی‌ماند — حتی با kill سخت (Job Object ویندوز).

## اجرای دائمی در پس‌زمینه

برای اینکه با بستن ترمینال قطع نشود:

```powershell
# پنجره‌ی مینیمایز
Start-Process python -ArgumentList "live.py","--skip-gate" -WindowStyle Minimized

# یا کاملاً مخفی، با لاگ در فایل
Start-Process python -ArgumentList "live.py","--skip-gate" `
  -RedirectStandardOutput "live.log" -WindowStyle Hidden
```

برای اجرای خودکار بعد از روشن شدن ویندوز، یک Task در Task Scheduler بسازید که
`python live.py --skip-gate` را با «Start in» روی پوشه‌ی پروژه اجرا کند.

## تست‌ها

```powershell
python test_live.py                # ۴۹ تست، شامل مقیاس ۲۰۰هزارتایی (~۴ دقیقه)
python test_live.py --skip-scale   # ۴۵ تست، چند ثانیه
```

تست‌ها به شبکه یا `xray.exe` نیاز ندارند و پوشه‌ی موقتشان را خودشان پاک می‌کنند.

## عیب‌یابی سرویس

| نشانه | علت و راه‌حل |
|---|---|
| `تلگرام: نشست معتبر نیست` | `session_string` منقضی شده — دوباره بسازید |
| `کانال X: محدودیت نرخ` | تلگرام موقتاً بست؛ دور بعد خودکار تلاش می‌کند |
| `برای تست: 0` | همه‌ی کانفیگ‌ها تازه تست شده‌اند — `retest_after_hours` را کم کنید |
| `استخر خالی است` | هنوز کانفیگ سالمی پیدا نشده؛ اول یک دور کشف بزنید |
| همه‌ی کانفیگ‌ها سالم | فیلترشکن روشن مانده — اعداد بی‌معنا هستند، دوباره با دروازه اجرا کنید |
| دور خیلی طولانی | `max_untested_per_cycle` یا `batch_test_size` را کم کنید |

## ساختار سرویس

```
live.py                 نقطه‌ی ورود سرویس
test_live.py            ۴۹ تست
vtester/
  live.py               دو چرخه، انتخاب کاندیدها، هماهنگی
  store.py              SQLite: کانفیگ‌ها، نتایج، سلامت، هرس
  telegram_source.py    دریافت از کانال‌ها با کش پیام
  vpngate.py            دروازه‌ی فیلترشکن با تشخیص خودکار IP
  pool.py               استخر top-N، فایل‌های دسته‌ای، تاریخچه
```

بقیه‌ی ماژول‌ها بین `run.py` و `live.py` مشترک‌اند.

